PORT=3001
# Comma-separated allowed hostnames for the stream proxy (e.g. your NAS).
# Leave empty to allow any http/https URL (not recommended on a public network).
ALLOWED_HOSTS=192.168.1.100,nas.local,localhost
